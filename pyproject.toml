[project]
name = 'pyfend'
dynamic = ['version']
requires-python = '>=3.7'
dependencies = [
	'maturin',
]
classifiers = ['Programming Language :: Rust']

[build-system]
requires = ['maturin']
build-backend = 'maturin'

[tool.maturin]
features = ['pyo3/extension-module']
